#ifndef EASYFIND
#define EASYFIND

class easyfind
{
      private:
        int n{0};

      public:
        easyfind(void);
        easyfind(/* Parameters */);
        easyfind(const easyfind &other);
        easyfind &operator=(const easyfind &other);
        ~easyfind();
};

#endif
