#include "DiamondTrap.hpp"

int main()
{
    // std::cout << "\n========== 1. PARAMETERIZED CONSTRUCTION ==========\n";
    // DiamondTrap a("Assan");

    // std::cout << "\n========== 2. WHO AM I ==========\n";
    // a.whoAmi();

    // std::cout << "\n========== 3. ATTACK (SCAVTRAP VERSION) ==========\n";
    // a.attack("Enemy");


    // std::cout << "\n========== 4. ATTRIBUTE CHECK ==========\n";
    // std::cout << "HitPoints    (FragTrap) : " << a.get_hit() << std::endl;
    // std::cout << "EnergyPoints (ScavTrap) : " << a.get_energy() << std::endl;
    // std::cout << "AttackDamage (FragTrap) : " << a.get_attack() << std::endl;

    // std::cout << "\n========== 5. COPY CONSTRUCTOR ==========\n";
    // DiamondTrap b(a);
    // b.whoAmi();
    // b.attack("CopyEnemy");

    // std::cout << "\n========== 6. COPY ASSIGNMENT ==========\n";
    // DiamondTrap c("Other");
    // c = a;
    // c.whoAmi();
    // c.attack("AssignedEnemy");

    // std::cout << "\n========== 7. ENERGY CONSUMPTION TEST ==========\n";
    // for (int i = 0; i < 55; i++)
    //     a.attack("Drain");

    // std::cout << "\n========== 8. DESTRUCTION ==========\n";
    // return 0;


	// DiamondTrap alpha("boss");
	// std::cout << "hit " << alpha.get_hit() << " energy " << alpha.get_energy() << " attack " << alpha.get_attack() << std::endl;
	// alpha.attack("enemy");
	// std::cout << "hit " << alpha.get_hit() << " energy " << alpha.get_energy() << " attack " << alpha.get_attack() << std::endl;
	// alpha.beRepaired(10);
	// std::cout << "hit " << alpha.get_hit() << " energy " << alpha.get_energy() << " attack " << alpha.get_attack() << std::endl;
	// alpha.takeDamage(50);
	// std::cout << "hit " << alpha.get_hit() << " energy " << alpha.get_energy() << " attack " << alpha.get_attack() << std::endl;
	// alpha.whoAmi();
	
	// DiamondTrap a("helo");

	// a.guardGate();

	// std::cout << "hit " << a.get_hit() << "energy " << a.get_energy() << "attack " << a.get_attack() << std::endl;


	// alpha.highFiveGuys();
	// beta.highFiveGuys();
	// gamma.highFiveGuys();

	// DiamondTrap delta;
	// DiamondTrap epsilon;
	// delta = epsilon = alpha;
	// delta.highFiveGuys();
	// epsilon.highFiveGuys();

	// DiamondTrap zeta(FragTrap("ZetaTemp"));
	// zeta.highFiveGuys();

	// alpha.attack("Enemy1");
	// beta.attack("Enemy2");
	// gamma.attack("Enemy3");

	// alpha.highFiveGuys();
	// beta.highFiveGuys();
	// gamma.highFiveGuys();
}
